#include <stdio.h>

/**
 * main - Prints "with good grammar, but the outcome is
 * a perfect art," followed by a new line.
 * Return: Always 0.
 */
int main(void)
{
	printf("with good grammar, but the outcome is a perfect art,\n");
	return (0);
}
