#include "main.h"

/**
 * _islower - check the code.
 * @c: the character
 * Description: check for lower case character
 *
 * Return: Always 0.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
